<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="textStyleMedium">
        <item name="android:layout_marginStart">@dimen/list_margin</item>
        <item name="android:layout_marginTop">@dimen/list_margin</item>
        <item name="android:textAppearance">?android:textAppearanceMedium</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/text_medium</item>
    </style>

    <style name="textStyleSmall">
        <item name="android:layout_marginBottom">@dimen/list_margin</item>
        <item name="android:layout_marginStart">@dimen/list_margin</item>
        <item name="android:textSize">@dimen/text_small</item>
    </style>

    <style name="splitterStyle">
        <item name="android:layout_width">1dp</item>
        <item name="android:layout_height">16dp</item>
        <item name="android:layout_marginStart">@dimen/image_list_margin</item>
        <item name="android:layout_marginBottom">@dimen/list_margin</item>
        <item name="android:background">@android:color/darker_gray</item>
    </style>

    <style name="imageStyle">
        <item name="android:layout_width">@dimen/image_list_size</item>
        <item name="android:layout_height">@dimen/image_list_size</item>
        <item name="android:layout_marginBottom">@dimen/image_list_margin</item>
        <item name="android:layout_marginStart">@dimen/image_list_margin</item>
        <item name="android:layout_marginTop">@dimen/image_list_margin</item>
        <item name="android:scaleType">centerCrop</item>
    </style>

</resources>
